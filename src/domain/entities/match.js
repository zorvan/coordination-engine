module.exports = { MatchState: { proposed: 'proposed', confirmed: 'confirmed', completed: 'completed', cancelled: 'cancelled' }, MatchStateMachine: { proposed: ['confirmed', 'cancelled'], confirmed: ['completed', 'cancelled'], completed: [], cancelled: [] }, createMatch: function(id, organizerId, title, description, scheduledTime, durationMinutes, location, participantIds) { participantIds = participantIds || []; return { id: id, state: 'proposed', organizerId: organizerId, title: title, description: description, scheduledTime: scheduledTime, durationMinutes: durationMinutes, location: location, participantIds: participantIds, createdAt: new Date(), updatedAt: new Date() }; }, canTransitionTo: function(currentState, targetState) { const transitions = MatchStateMachine[currentState]; return transitions ? transitions.includes(targetState) : false; } };